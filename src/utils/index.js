// this function convert decimeters to Centimeters
export const toCentimeters = (val) => {
  return val * 10;
};

// this function convert hectograms to kilograms
export const toKilograms = (hg) => {
  return hg / 10;
};
